{
  "name": "my-netlify-app",
  "version": "1.0.0",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "scripts": {
    "build": "mkdir -p dist && echo 'web dir' > dist/index.html"
  }
}
